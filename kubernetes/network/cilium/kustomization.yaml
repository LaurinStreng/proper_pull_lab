apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: network

helmCharts:
- repo: https://helm.cilium.io
  name: cilium
  version: 1.16.5
  includeCRDs: true
  releaseName: cilium
  valuesFile: values.yaml
